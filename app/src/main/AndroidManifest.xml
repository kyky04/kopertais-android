<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uinbdg.skripsi.kopertais">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.DetailUnivActivity" />
        <activity android:name=".Activities.PerjalananActivity" />
        <activity android:name=".Activities.MapsActivity" />
        <activity android:name=".Activities.MasterUnivActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Activities.MasterPerjalananActivity" />
        <activity android:name=".Activities.MasterRekomendasiActivity" />
        <activity android:name=".Activities.MasterPerjalananDinasActivity" />
        <activity android:name=".Activities.MenuActivity" />
        <activity android:name=".Activities.TambahRekomendasiActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Activities.InputPerjalananDinasActvity"></activity>
    </application>

</manifest>